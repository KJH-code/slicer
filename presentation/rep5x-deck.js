/**
 * 중간 성과공유회 발표용 발표자 메모.
 * presentation.js 보다 먼저 로드되며, 이미 정의돼 있으면 기본 메모로 덮지 않는다.
 * 화면에는 안 나오고 콘솔에서 speakerNotes[번호]로 본다.
 */
window.speakerNotes = Object.freeze({
  1: `한 줄 요약: 상반기에는 '무엇으로 판정하는가'를 세웠고, 이번에는 그 판정이 어떤 기계를 향하는지를 정했다.
제목은 등록명을 유지한다(등록명 "4축", 실기 계열은 5축).
[Sources] KJH-code/slicer README`,

  2: `이 발표의 뼈대다. 왼쪽이 저번에 우리가 스스로 제기한 문제, 오른쪽이 지금 상태.
위 셋은 해결, 넷째가 지금 하는 것, 다섯째는 다음이다 — 이 순서가 그대로 3~12쪽 순서다.
다섯째 줄에서 "실물 제작은 지금 불가능하다"를 분명히 말한다. 숨기지 않고 먼저 밝히는 편이 낫다.
[Sources] 팀 통합 노트의 '지난 발표 대비 현재' 표`,

  3: `계층을 위에서 아래로 읽지 말고 각 층이 '못 하는 것'을 강조한다. 그 문장이 이 설계의 핵심이다.
오른쪽 산점도의 초록 점 하나가 ②층이 실제로 일한 사건이다 — 예측 0.5%를 검사기가 25.5%로 잡았다.
③층의 '완전 독립이 아니다'를 스스로 말해야 ⑤층(사람의 독립 구현)의 필요가 설명된다.
[Sources] docs/verification.md, compare_prediction_vs_toolpath.py, compare_with_teammate.py`,

  4: `핵심 문장: "항상 이기는 게 아니라 이기는 조건이 있다." 그 조건이 '허리'다.
왼쪽 그림 — 구는 밴드가 균일각과 같은 값(1.08%)으로 수렴, 램프는 3.01% → 1.78%.
오른쪽 그림 — N을 늘려도 구는 평평하다. 이 단조성은 점검으로 걸어뒀고, 실제로 깨지면서 탐색 결함 셋을 찾았다.
κ 규칙 이견은 "누가 옳나"가 아니라 "어느 눈금에서 만나나"로 풀었다.
[Sources] compare_waist.py, compare_bands.py, compare_kappa_rules.py`,

  5: `일곱 단계를 다 설명하지 말고 '실패에서 배운 것'만 짚는다.
01 축 센터링 — 실제 STL은 대부분 축 밖이라 변환이 무의미해졌던 사건.
05 역변환 — 고정 분할로 G-code가 100만 줄이 됐던 사건.
아래 세 카드는 연구 방법 이야기다. 특히 세 번째(가중치를 값이 아니라 '창'으로)를 강조한다.
[Sources] Architecture.md, Bugs.md, analyze_blend_k.py`,

  6: `전환점. 먼저 "실물 제작은 지금 불가능하다"를 인정한다.
그다음 왜 그런데 프린터 설계가 이번 핵심인지 — 기계를 정하지 않으면 최대 안전각·충돌 기하·G-code 형식이 전부 가정값이기 때문이다.
"슬라이서를 재발명하지 않았듯 프린터도 재발명하지 않는다"가 이 연구의 결정 방식이다.
[Sources] Decisions.md, Rep5x README`,

  7: `홈페이지는 링크로 열지 않고 화면 재현으로 보여준다(오프라인 대비). 문구는 저장소 website/ 의 실제 텍스트다.
오른쪽 '왜 이 설계인가'가 9·10쪽으로 이어지는 고리다 — 헤드 회전 + 펌웨어 IK.
[Sources] Rep5x/website/src/content, Rep5x README`,

  8: `여기서는 기계 전체의 생김새와 '우리가 얻은 숫자'만 말한다. 부품별 3D 모델은 다음 쪽.
요점 둘 — (1) XYZ는 그대로 두고 핫엔드 쪽만 바꾼다, (2) 기종 의존이 마운트 하나에 격리돼 있다.
오른쪽 숫자들이 전에는 전부 가정값이었다는 점을 반드시 말한다.
사진은 전부 Rep5x 저장소의 원저자 촬영본이다.
[Sources] Rep5x build-guide, firmware Configuration.h`,

  9: `왼쪽 목록을 눌러 부품을 전환하며 설명한다. 로컬 HTTP 서버로 열어야 3D 모델이 자동으로 뜬다.
중요한 셋만 봐도 된다 — B암(베어링·원점), 슬립링 홀더(연속 회전의 조건), 캐리지 마운트(기종 의존이 여기 모임).
3D 모델은 저장소의 3MF를 STL로 변환해 표시용으로 넣은 것이다.
[Sources] Rep5x build-guide 3d-printed-parts, CHANGELOG.md`,

  10: `기계와 우리 코드를 잇는 자리.
대비가 핵심 — Open5x는 슬라이서가 기계좌표를 계산해야 했고, Rep5x는 펌웨어가 역기구학을 하므로 노즐 끝 좌표 + B/C 각도만 내면 된다.
슬립링 덕분에 Open5x의 최대 난점(배선 감김, 구 데모 149회전)이 사라진다.
아래 애니메이션은 위 코드와 같은 식을 실제로 계산해 그린 것이다. '일시정지'로 멈춰 한 자세에서 설명할 수 있다.
읽어주는 순서: ① 청록 링이 원뿔 레이어면 ② 주황 선이 노즐 축, 그 뒤가 이미 압출한 경로 ③ 왼쪽 위 숫자와 아래 G-code 한 줄이 실시간 값.
코드는 설계안이며 아직 구현·검증 전이다(11쪽 02번).
[Sources] conical/open5x.py 주석, Rep5x 펌웨어 문서`,

  11: `실패 목록이 아니라 '다음에 확인할 항목'으로 읽는다.
01은 제작 불가에서 오는 것이고, 02·03·04는 코드가 3축/베드틸트 가정에 묶여 있다는 하나의 문제의 세 얼굴이다.
05·06은 상반기부터 알고 코드와 문서에 적어 둔 한계다.
[Sources] TODO.md, Bugs.md, Decisions.md`,

  12: `1순위가 이 연구의 다음 한 걸음이다 — 설계만 되어 있는 기구학 모듈을 짜고 손계산과 대조한다.
마지막 카드에서 실물 출력은 '여건이 생기면'이라고 분명히 말한다. 계획으로 약속하지 않는다.
목표 문장: "더 좋은 각도"가 아니라 "어디까지 믿을 수 있는 각도인가".
[Sources] TODO.md, Overview.md 정직성 규약`,

  13: `무엇이 우리 것이 아닌지를 먼저 말한다 — 원뿔 변환식(RotBot), 복잡도–성능 곡선(Wu 2020), 프린터 설계(Rep5x).
우리 축은 '연속 원뿔각을 자동으로, 가볍고 투명하게 고르는 것' 하나다.
Rep5x 자료는 GPL v3이고 저작권은 원저작자에게 있다.
[Sources] docs/prior_work_analysis.md`
});
